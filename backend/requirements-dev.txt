# 개발·시험 전용 의존성. 운영 이미지엔 안 들어간다.
#
#   python -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
#   (윈도우는 .venv/Scripts/pip)
#
# 운영 이미지는 requirements.txt만 설치하고, Dockerfile이 빌드 끝에 pytest가 없는지
# 실제로 확인한다. 그래서 여기에 얹는 도구가 운영 이미지로 새지 않는다.
#
# ⚠ 여기도 간접까지 `==`로 못박는다. 시험이 이미지와 다른 판으로 돌면 초록이 증거가
#   못 된다 — 2026-08-05 빌드 #74 때 개발자 venv는 Mako 1.3.12였고 이미지만 1.4.0이라,
#   시험을 몇 번을 돌려도 그 사고를 만날 수 없었다.
-r requirements.txt

# ── 직접 의존성 (우리가 골라서 쓰는 것) ──
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-xdist==3.8.0
jsonschema==4.23.0

# ── 간접 의존성 (위 것들이 딸고 오는 것) ──
attrs==26.1.0                        # jsonschema, referencing
execnet==2.1.2                       # pytest-xdist
iniconfig==2.3.0                     # pytest
jsonschema-specifications==2025.9.1  # jsonschema
packaging==26.2                      # pytest
pluggy==1.6.0                        # pytest
referencing==0.37.0                  # jsonschema, jsonschema-specifications
rpds-py==2026.6.3                    # jsonschema, referencing
